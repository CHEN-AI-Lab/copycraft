{
  "name": "copycraft",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "pnpm --filter web tsc --noEmit && cd shared && npx tsc --noEmit",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "setup": "pnpm install",
    "deploy": "echo 'Deploy: push to GitHub, import in Vercel'"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "vitest": "^3.2.4"
  }
}