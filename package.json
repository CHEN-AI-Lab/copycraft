{
  "name": "copycraft",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:light": "pnpm vitest run --pool=forks --poolOptions.forks.singleFork --maxWorkers=1",
    "typecheck": "pnpm --filter web exec -- tsc --noEmit && pnpm --filter shared exec -- tsc --noEmit",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "check:json": "python3 scripts/check-json.py",
    "check:light": "pnpm check:json && pnpm typecheck && pnpm test:light",
    "setup": "pnpm install",
    "deploy": "echo 'Deploy: push to GitHub, import in Vercel'"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@prisma/client": "^7.8.0",
    "@types/node": "^22.19.19",
    "prisma": "^7.8.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}